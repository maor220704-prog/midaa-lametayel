# לקחים - מידע למטייל

<!-- טעויות שתוקנו, כדי שלא יחזרו. -->
<!-- החוק: תיקון נשמר קודם בהוראה הראשית (CLAUDE.md או הנוהל), ורק אז נרשם כאן. -->
<!-- שורה כאן בלי תיקון בהוראה = הטעות תחזור. -->
<!-- את הקובץ הזה מעדכן /save בסוף כל סשן - אין צורך לכתוב ידנית. -->

| תאריך | מה קרה | איפה תוקן | הכלל מעכשיו |
|---|---|---|---|
| (בבניית האפליקציה) | קלאס CSS מותאם אישית (`.nav-glass`) הגדיר `position: relative` שדרס בשקט את `fixed` של Tailwind - הסרגל התחתון הפסיק להיות דביק באתר החי | `src/index.css` (הערה מפורטת ליד `.nav-glass` ו-`.header-elevated`) | לא להוסיף `position` בקלאס מותאם אישית אלא אם באמת צריך - זה יכול לדרוס יוטיליטי של Tailwind |
| (בבניית האפליקציה) | אנימציית hover/focus על כפתורי הבחירה רצה גם ממגע במובייל, ונשארה "תקועה" אחרי גלילה | `src/index.css` (`.glow-halo` - guard מאחורי `@media (hover: hover) and (pointer: fine)`) | הנפשה שקשורה ל-hover (לא ל-state נבחר) - תמיד מאחורי hover-media-query, אף פעם לא ישירות על `:hover` |
| (בבניית האפליקציה) | כפתור ניווט/מידע השתמש ב-`window.open`+`setTimeout` כדי "לחכות" לאנימציית לחיצה לפני שפותח קישור - נשבר לגמרי במובייל ובאפליקציה מותקנת | `src/components/PlaceCard.tsx` (קישור `<a href target="_blank">` פשוט, בלי שום יירוט ב-JS) | קישור חיצוני = `<a>` רגיל תמיד. לעולם לא לעכב ניווט ב-JS כדי "לחכות" לאנימציה |
| 31.8 | הרצת PowerShell `Get-Content -Raw \| Set-Content` להחלפת טקסט בקובץ השחיתה (mojibake) את כל הטקסט העברי בו | שוחזר מ-git (`git checkout`), ההחלפה בוצעה מחדש עם כלי Edit | טקסט בקבצים עם עברית - תמיד כלי Edit להחלפות, לעולם לא PowerShell Get-Content/Set-Content |
