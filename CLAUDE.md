# מידע למטייל

מדריך טיול PWA בעברית/RTL לטיול משפחתי בוייטנאם ותאילנד. React 19 + TypeScript + Vite + Tailwind v4, ללא בקאנד - סטטי לגמרי, PWA, פרוס על GitHub Pages (`maor220704-prog/midaa-lametayel`, חי ב-https://maor220704-prog.github.io/midaa-lametayel/). יש לו פרויקט "אח" ליפן ב-`../אפליקציה ליפן` שבנוי מאותה ארכיטקטורה ומערכת עיצוב - שני פרויקטים נפרדים לגמרי, אף שינוי לא אמור לחצות ביניהם.

## הנחיות
- כל מקום/מסעדה/אטרקציה חייב `sources` אמיתי בקוד (`src/data/cities/*.ts`) - אין המצאת תוכן. "לא הצלחתי לאמת" זו תשובה לגיטימית, לא ממציאים כדי למלא מכסה.
- לעולם לא להמציא `coords` - כפתור הניווט תמיד נבנה מ-`nameLatin` + שם עיר.
- אין אימוג'י בשום תוכן משתמש.
- לפני שמדווחים שמשהו "מוכן" - לבדוק בפועל: build+lint+דפדפן. לא מספיק שהקוד נראה נכון.
- לפני `git push` לפרודקשן - לבקש אישור מפורש בצ'אט כל פעם. אישור פעם אחת לא חל אוטומטית על הפעם הבאה.
- משימות מחקר/בנייה גדולות (מחקר תוכן לכמה ערים, בניית פיצ'ר שלם) - להאציל לסוכני רקע (Agent tool) כדי לחסוך טוקנים בשיחה הראשית. זו העדפה מפורשת שנשמרה כמה פעמים.
- שינוי עיצובי (צבעים, קומפוננטה חדשה) - קודם מוקאפ HTML לאישור, ורק אז קוד אמיתי.
- טקסט לקבצים עם עברית - תמיד עם כלי Edit, לעולם לא PowerShell `Get-Content`/`Set-Content` (משחית קידוד).

## מפה - מה יש בתיקייה
- `PRD.md` - מסמך דרישות המוצר המקורי
- `design.md` - כללי העיצוב, כולל כלל "warm parchment" (שקיפות רק מעל רקע שהאפליקציה שולטת בו, לעולם לא מעל תמונה)
- `CLAUDE_CODE_PROMPT.md` - ההנחיה המקורית שהתחילה את הפרויקט (היסטורי, לא נקרא שוטף)
- `src/data/cities/*.ts` - תוכן לכל עיר (7 ערים), מאומת עם מקורות
- `src/data/packingList.ts` - רשימת ציוד מותאמת אקלים - בסיס לכל הטיול + סעיף לכל עיר
- `src/index.css` - מערכת העיצוב המלאה: טוקנים + קומפוננטות (`.nav-glass`, `.header-elevated`, `.glow-halo`, `.tab-chip-active`, `.packing-card`, `.packing-card--pulse`, `.packing-panel`, `.packing-check`)
- `.github/workflows/deploy.yml` - דיפלוי אוטומטי ל-GitHub Pages בכל push ל-master
- `status.md` - איפה עצרנו
- `lessons.md` - טעויות שתוקנו
- `examples.md` - דוגמאות לתוצאה טובה

## סדר קריאה
- בתחילת כל שיחה: `status.md` קודם
- לפני שינוי עיצובי: `design.md`
- שאר הקבצים - לפי הצורך
